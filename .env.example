# SECURITY REQUIREMENTS
# Session Secret (REQUIRED - app will not start without this)
# Generate a random string for production (min 32 characters)
# Example: openssl rand -base64 32
SESSION_SECRET=your_random_session_secret_here_min_32_chars

# Admin Credentials (REQUIRED)
# Required for accessing the admin panel at /idara
# These credentials are hashed and stored securely
# Note: Registration endpoint has been removed for security
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_secure_password

# SMS Notification Service (Bulkgate)
# Get these credentials from https://portal.bulkgate.com/
# Optional - only needed if you want to send SMS notifications
BULKGATE_APP_ID=your_bulkgate_app_id
BULKGATE_APP_TOKEN=your_bulkgate_app_token

# SECURITY NOTES:
# - Admin login is rate-limited to 5 attempts per 15 minutes per IP
# - Failed login attempts are logged with IP addresses
# - robots.txt blocks search engines from indexing admin paths
# - Session cookies are httpOnly and secure in production
# - No registration endpoint - admin users cannot be created via API

# Note: In Replit, add these as Secrets instead of using a .env file
# Go to the Secrets tab and add each variable there
